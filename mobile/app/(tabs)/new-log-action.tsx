import { View } from "react-native";

// Placeholder route — never rendered. The center tab button in (tabs)/_layout.tsx
// intercepts onPress and routes to /log/new (modal) instead.
export default function NewLogAction() {
  return <View />;
}
